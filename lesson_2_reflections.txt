What happens when you initialize a repository? Why do you need to do it?
When I initialize a repository git create a hidden directory where it's storing
a metadata needed to track history of files. Why do I need it? I have no idea.